{
  "name": "operator-proof-demos",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "node scripts/generate-fixtures.mjs",
    "test": "node scripts/validate.mjs",
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "4.24.0"
  }
}
